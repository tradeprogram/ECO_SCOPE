# --- 지리·원격탐사 ---
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
rasterio>=1.3
earthengine-api>=1.0

# --- 자료 처리 ---
pandas>=2.2
numpy>=1.26

# --- 질의응답 서버 (Vercel Python Functions) ---
# 외부 SDK 없이 표준 라이브러리만으로 호출한다. 런타임 의존성을 늘리지 않기 위함.
